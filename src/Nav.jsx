import Button from './Button.jsx'

function Nav() {
    return (
        <div style={{justifySelf: "left", padding: "1rem"}}>
            <Button path={"home"}>Home</Button>
        </div>
    );
}

export default Nav;